<br />
<h3><%= @pattern.name %></h3>

<h4>Materials and notions:</h4>
  <%= @pattern.materials %>

<h4>Knitting Needles:</h4>
  <%= @pattern.needles %>

<h4>Yarns:</h4>
  <p><%= @pattern.yarn %></p>
  <p>Yarn weight: <%= @pattern.weight %></p>
  <p>Yarn quantity: <%= @pattern.quantity %></p>

<h4>Directions: </h4>
  <p><%= @pattern.content %></p>

<h3>Comments</h3>
  <p><%= link_to "Add a Comment", new_pattern_comment_path(@pattern) %></p>
    <ul class="comment_list">
      <p><%= render partial: "comments/comment", collection: @pattern.comments %></p>
    </ul>

<h4>Check here if you want to 'favorite' this pattern: </h4>

  <%= form_tag @pattern, method: :post do %>
    <%= label :pattern, "Favorite Pattern" %>
    <%= check_box(@pattern, :pattern_id, {checked: false}) %>
  <% end %>
<br />

This pattern has been favorited # times. <br>

<% if current_user == @pattern.user %>
  <br />
  <%= link_to "Edit this Pattern", edit_pattern_path %>
  <br />
  <%= link_to "Destroy", pattern_path, method: :delete, alert: { confirm: "Are you sure?" } %>
<% end %>

  <br />
  <%= link_to "Back to Pattern List", patterns_path %>
