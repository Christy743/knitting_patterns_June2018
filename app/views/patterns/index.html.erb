
Categories:<br />
<ul>
  <% @categories.each do |category| %>
  <li><%= category.name %></li>
  <% end %>
</ul>

<h3>Pattern List</h3>

<br />

<ul>
  <% @patterns.each_with_index do |pattern, i| %>
    <%= i + 1 %>. <%= link_to pattern.name.capitalize, pattern_path(pattern.id) %><br></br>
  <% end %>
</ul>

<br />
<br />

<% if current_user %>
  <%= link_to "New Pattern", new_pattern_path %>
<% end %>

<% if current_user.patterns.count == 0 %>
  <p>You have no projects. Click on 'New Pattern' to create a new knitting pattern.</p>
<% end %>
