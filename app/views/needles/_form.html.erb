<div class="row">
  <div class="col-md-6 col-md-offset-3">
<%= form_for (@needle) do |f| %>
  <% if @needle.errors.any? %>
    <div id="error_explanation">
      <div class="alert alert-danger">
    <h3><%= pluralize(@needle.errors.count, "error") %> prohibited this pattern from being saved: </h3>
      </div>
      <ul>
        <% @needle.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :name %>
  <%= f.text_field :name, class: "form-control" %>
  <br></br>
  <%= f.label :us_size %>
  <%= f.text_field :us_size, class: "form-control" %>

  <%= f.submit class: "btn btn-primary" %>
<% end %>
</div>
</div>
